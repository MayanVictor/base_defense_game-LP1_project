{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Raylib",
            "type": "shell",
            "command": "C:/msys64/ucrt64/bin/g++.exe",
            "args": [
                "the_invasion.cpp",
                "game.cpp",
                "-o",
                "the_invasion.exe",
                "-I/ucrt64/include",
                "-L/ucrt64/lib",
                "-lraylib"
            ],
            "options": {
                "cwd": "${fileDirname}",
                "env": {
                    "PATH": "C:/msys64/ucrt64/bin;${env:PATH}"
                }
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}